<h1>Lista de cámaras de colegios</h1>
<br> 
  <%= link_to 'Agregar cámara', new_unit_temporal_path, class: "btn btn-info btn-sm" %>
  <%= render "layouts/inicio" %>
<hr>

<%= form_tag import_unit_temporals_path, multipart: true do %>
<div class="form-group">
  <div class="row">
    <div class="col-md-3 col-lg-3">
      <%= file_field_tag :file, required: true, class: "btn btn-sm btn-info" %>
      <p class="help-block">Nota: La extensión del archivo debe ser .csv</p>
    </div>
    <div>
      <%= button_tag(type: 'submit', class: "btn btn-info") do %>  
        <i class="fa fa-arrow-circle-up"></i>
      <% end %>
    </div>
  </div>
</div>  

<% end %>

<%= render "layouts/buscar" %>
<div class="row">
  <div class="col-lg-12">
    <table class="table" id="table">
      <thead>
        <tr>
          <th>Polígono</th>
          <th>Punto</th>
          <th>Orientación</th>
          <th>Dirección</th>
          <th>Localidad</th>
          <th>Cosec</th>      
          <th>Condición</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody class="searchable">
        <% @unit_temporals.each do |unit_temporal| %>
          <tr>
            <td><%= unit_temporal.polygon ? poligono : no_poligono %></td>
            <td><%= unit_temporal.spot %></td>
            <td><%= unit_temporal.orientation %></td>
            <td><%= unit_temporal.address %></td>
            <td><%= unit_temporal.location %></td>
            <td><%= unit_temporal.cosec %></td>            
            <td class="text-center">
              <% if unit_temporal.condition? %>
                  <div class="col-sm-1">
                      <div class="round round-sm green">
                          <%= bueno %>
                      </div>
                  </div>
              <% else %>
                  <div class="col-sm-1">
                      <div class="round round-sm red">
                          <%= malo %>
                      </div>
                  </div>        
              <% end %>
            </td>
            <td><%= link_to unit_temporal do %>
                  <i class="fa fa-search"></i>
                <% end %>
            </td>
            <td><%= link_to edit_unit_temporal_path(unit_temporal) do %>
                  <i class="fa fa-pencil"></i>
                <% end %>
            </td>
            <td><%= link_to unit_temporal, method: :delete, data: { confirm: 'Are you sure?' }  do %>
                  <i class="fa fa-trash"></i>
                <% end %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>

<br>

<%= link_to 'Agregar cámara', new_unit_temporal_path, class: "btn btn-info btn-sm" %>
<%= render "layouts/inicio" %>